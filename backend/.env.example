# Copy to .env and fill in your values
PORT=3000
NODE_ENV=development

# MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=Manlikearis23$
DB_NAME=pos_db

# JWT (use a long random string in production)
JWT_SECRET=your_jwt_secret_key_here
