# Copy from .env.example. REQUIRED: set DB_PASSWORD to your MySQL root password.
PORT=3000
NODE_ENV=development

# MySQL - use the password you set for 'root' during MySQL installation
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=Manlikearis23$
DB_NAME=pos_db

# JWT (use a long random string in production)
JWT_SECRET=pos_jwt_secret_change_in_production
